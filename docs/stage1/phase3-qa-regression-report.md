# 第三阶段回归报告（M2-M5 已封板）

> 状态：M2、M3、M4、M5 完成
>
> 本文档已记录 M2 硬约束回归、M3 真机静态风格切换回归、M4 UI 真机观感回归，以及 M5 数据策略与全量回归收口结论。

## M2 本地回归（代码级）

执行时间：2026-03-30  
执行方式：本地 Python 脚本调用 `media_generate_service`。

### 场景 1：拒绝授权

- 输入：`consent_confirmed=false`，`MEDIA_GEN_REQUIRE_CONSENT=1`
- 结果：返回 `MEDIA_GEN_CONSENT_REQUIRED`
- 结论：通过

### 场景 2：超周限额

- 输入：同用户在同周内第 2 次创建任务，`MEDIA_GEN_WEEKLY_LIMIT=1`
- 结果：返回 `MEDIA_GEN_WEEKLY_LIMIT_EXCEEDED`
- 结论：通过

### 场景 3：积分不足

- 输入：`MEDIA_GEN_POINTS_COST=999`，默认余额不足
- 结果：返回 `MEDIA_GEN_POINTS_INSUFFICIENT`
- 结论：通过

### 场景 4：失败回滚

- 输入：先扣分后强制触发 provider 失败（非法 provider）
- 结果：任务失败，积分恢复到失败前（`points_restored=True`），且可再次创建任务
- 结论：通过

## M2 线上回归（云托管环境）

执行时间：2026-03-31  
执行方式：直接调用云托管正式访问域名 `https://emotion-culture-api-237560-9-1415063583.sh.run.tcloudbase.com`

### 场景 1：拒绝授权

- 请求：`consent_confirmed=false`
- 返回：`403 MEDIA_GEN_CONSENT_REQUIRED`
- 结论：通过

### 场景 2：超周限额

- 请求：同一用户在同一自然周内连续创建两次任务
- 返回：第 1 次 `200`，第 2 次 `429 MEDIA_GEN_WEEKLY_LIMIT_EXCEEDED`
- 结论：通过

### 场景 3：积分不足

- 前置配置：`MEDIA_GEN_POINTS_COST=999`
- 返回：`402 MEDIA_GEN_POINTS_INSUFFICIENT: need=999, current=12`
- 结论：通过

### 场景 4：失败回滚

- 前置配置：`MEDIA_GEN_PROVIDER=invalid_provider`、`MEDIA_GEN_POINTS_COST=12`
- 第 1 步返回：创建任务 `200`，随后任务终态 `failed`
- 失败原因：`MEDIA_GEN_PROVIDER_DISABLED`
- 回滚验证：恢复 `MEDIA_GEN_PROVIDER=local_mock`、`MEDIA_GEN_POINTS_COST=1` 后，使用同一 `x-openid` 再次创建任务返回 `200`
- 结论：通过

## M2 封板结论

- M2 四类硬约束场景均已通过
- 当前可判定：
- `BE-304` 完成
- `BE-305` 完成
- `BE-306` 完成
- `BE-307` 完成
- `DATA-303` 完成
- M2 可封板，第三阶段进入 `M3`

## M3 真机回归（静态风格切换）

执行时间：2026-04-01
执行方式：真机扫描最新体验版二维码，前后端均发布到包含 `892ec22` 修复的版本。

### 场景 1：古典风切换

- 操作：进入结果页，点击 `古典风 -> 切换风格图`
- 结果：成功切换到诗人/唐宋静态图池图片
- 结论：通过

### 场景 2：国潮风切换

- 操作：进入结果页，点击 `国潮风 -> 切换风格图`
- 结果：成功切换到国潮静态图池图片
- 结论：通过

### 场景 3：科技风未配置图池

- 操作：进入结果页，点击 `科技风 -> 重试风格图`
- 结果：不再显示诗人图，不保留旧预览残留；显示中文提示 `科技风格图片暂未支持，请耐心等待后续发布。`
- 结论：通过

### 场景 4：删除自拍后继续切换

- 操作：先执行“从邮件移除”删除自拍，再点击 `古典风/国潮风 -> 切换风格图`
- 结果：切换功能仍可用，不再依赖自拍存在
- 结论：通过

### 场景 5：邮件主链路保护

- 操作：在风格切换成功或失败后继续执行邮箱发送
- 结果：邮件发送功能正常，不因风格切换失败受阻
- 结论：通过

## M3 封板结论

- M3 静态风格切换主链路真机回归已通过
- 当前可判定：
- `MINI-301` 完成
- `MINI-302` 完成
- `MINI-303` 完成
- `MINI-304` 完成
- `MINI-305` 完成
- `MINI-310` 完成
- M3 可封板，第三阶段进入 `M4`

## M4 真机回归（UI 重构收口）

执行时间：2026-04-01

执行方式：真机扫描最新体验版二维码，前后端均发布到包含 `a4005aa` 的版本。

### 场景 1：首页采集入口观感

- 观察点：顶部入口区、步骤 01-04、提交区层级是否清晰
- 结果：首页已具备“采集入口”结构，`先写一句 -> 再选补充 -> 提交分析` 路径清楚，按钮可见性正常
- 结论：通过

### 场景 2：结果页仪表盘观感

- 观察点：顶部摘要块、诗词陪伴卡、国潮陪伴卡、风格切换卡、邮箱卡、后续动作卡是否分区明确
- 结果：结果页信息层级较前版明显增强；情绪摘要、图片卡片、风格切换和邮箱投递均可区分；窄屏下风格按钮已修正为屏内完整显示
- 结论：通过

### 场景 3：分享页导出工作台观感

- 观察点：顶部说明、导出步骤、自拍预览区、卡片预览区、导出操作区是否形成完整流程
- 结果：分享页已形成“导出工作台”结构；操作按钮 2x2 布局稳定，预览区与导出区职责清晰
- 结论：通过

### 场景 4：真机视觉一致性与可用性

- 观察点：按钮是否可见、页面是否出现重叠/遮挡/错位
- 结果：此前出现的按钮不可见、布局偏移、日历标记异常、风格按钮越界等问题均已修复；本轮真机观感确认通过
- 结论：通过

## M4 封板结论

- M4 UI 重构真机观感回归已通过
- 当前可判定：
- `MINI-306` 完成
- `MINI-307` 完成
- `MINI-308` 完成
- `MINI-309` 完成
- 真机视觉一致性要求完成
- M4 可封板，第三阶段进入 `M5`

## M5 本地自动化回归（数据与域名策略收口 + 全量回归）

执行时间：2026-04-01

执行方式：仓库根目录执行 `./.venv/bin/python tools/phase3_qa_regression.py`。  
详细自动化用例明细见：

- [phase3-m5-qa-regression-report.md](/Users/babytech/github/emotion_culture/docs/stage1/phase3-m5-qa-regression-report.md)

### 场景 1：`DATA-301` 生命周期治理

- 操作：追踪 2 个 `cloud://` 媒体 file_id，手动改写为过期时间后执行清理
- 结果：过期媒体均被识别并删除，追踪文件无残留
- 结论：通过

### 场景 2：`DATA-302` 域名与云环境配置一致性

- 操作：校验小程序 `apiBaseUrl`、`cloudEnv`、`containerEnv`、`containerService`
- 结果：云托管域名为正式访问域名，`cloudEnv == containerEnv == prod-9gok8bmyd517976f`
- 结论：通过

### 场景 3：`QA-301` 图池命中回归

- 操作：对古典风注入“主情绪命中”素材，对国潮风注入“触发标签命中”素材，发起风格图任务
- 结果：古典风按情绪标签命中预期素材，国潮风按触发标签命中预期素材
- 结论：通过

### 场景 4：`QA-302` 弱网/失败模拟回归

- 操作：模拟 URL 下载超时并校验回退到 `file_id/本地素材`；再模拟科技风空池失败后继续执行分析与邮件发送
- 结果：URL 失败可回退；空池失败返回明确错误且不阻塞主分析与邮件
- 结论：通过

### 场景 5：`QA-303` 页面与邮件图片一致性

- 操作：使用分析接口返回的 `poet_image_url` / `guochao_image_url` 继续发送邮件
- 结果：邮件链路解析到的本地静态素材文件名与页面图片 URL 一致
- 结论：通过

### 场景 6：`QA-304` 风控约束专项回归

- 操作：复核拒绝授权、超周限额、积分不足、失败回滚四类场景
- 结果：返回码与回滚行为均符合 M2 约束，无回退
- 结论：通过

## M5 封板结论

- M5 自动化回归 `11/11` 通过
- 当前可判定：
- `DATA-301` 完成
- `DATA-302` 完成
- `QA-301` 完成
- `QA-302` 完成
- `QA-303` 完成
- `QA-304` 完成
- `QA-305` 完成
- M5 可封板，第三阶段整体封板完成
